apiVersion: v1
kind: Service
metadata:
  name: migration-tool-frontend
  namespace: NAMESPACE_PLACEHOLDER
  labels:
    app: migration-tool
    component: frontend
spec:
  selector:
    app: migration-tool
    component: frontend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
