apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: migration-tool-backend
  namespace: NAMESPACE_PLACEHOLDER
  labels:
    app: migration-tool
    component: backend
spec:
  lookupPolicy:
    local: false
